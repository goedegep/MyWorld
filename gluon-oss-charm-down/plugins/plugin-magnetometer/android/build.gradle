dependencies {
    compile project(':plugin-magnetometer')
    compile project(':plugin-accelerometer/android')
    compile project(':plugin-lifecycle')
    compile project(':plugin-lifecycle/android')
}

ext.description = 'API to access magnetometer features for the android platform'
