dependencies {
    compile project(':core/ios')
    compile project(':plugin-barcode-scan')
}

ext.description = 'API to access barcode scan features for the iOS platform'

def service = "BarcodeScan"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}
