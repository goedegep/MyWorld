dependencies {
    compile project(':core/ios')
    compile project(':plugin-share')
}

ext.description = 'API to access sharing features for the iOS platform'

def service = "Share"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}
