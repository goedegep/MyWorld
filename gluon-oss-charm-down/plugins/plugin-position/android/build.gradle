dependencies {
    compile project(':plugin-position')
    compile project(':plugin-lifecycle')
    compile project(':plugin-lifecycle/android')
}

ext.description = 'API to access position features for the android platform'
