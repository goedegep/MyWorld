dependencies {
    compile project(':plugin-orientation')
    compile project(':plugin-lifecycle')
    compile project(':plugin-lifecycle/android')
}

ext.description = 'API to access orientation features for the android platform'
