dependencies {
    compile project(':core/ios')
    compile project(':plugin-device')
}

ext.description = 'API to access device features for the iOS platform'

def service = "Device"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}