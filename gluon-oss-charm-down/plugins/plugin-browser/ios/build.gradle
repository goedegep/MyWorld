dependencies {
    compile project(':core/ios')
    compile project(':plugin-browser')
}

ext.description = 'API to access browser features for the iOS platform'

def service = "Browser"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}
