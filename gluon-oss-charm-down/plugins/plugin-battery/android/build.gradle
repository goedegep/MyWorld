dependencies {
    compile project(':plugin-battery')
    compile project(':plugin-lifecycle')
    compile project(':plugin-lifecycle/android')
}

ext.description = 'API to access battery features for the android platform'
